import CodeTab from '@mdx/CodeTab.astro';
import CodeTabs from '@mdx/CodeTabs.astro';
import Section from '@mdx/Section.astro';
import Tab from '@mdx/Tab.astro';
import Tabs from '@mdx/Tabs.astro';
import Callout from '@mdx/Callout.astro';
import Npm from '@mdx/Npm.astro';
import Npx from '@mdx/Npx.astro';
import Steps from '@mdx/Steps.astro';
import Prerequisites from "@mdx/Prerequisites.astro"

# 使用 Drizzle Kit 进行迁移
<Prerequisites>
- 开始使用 Drizzle 和 `drizzle-kit` - [点此阅读](/docs/mssql/get-started)
- Drizzle schema 基础 - [点此阅读](/docs/mssql/sql-schema-declaration)
- 数据库连接基础 - [点此阅读](/docs/mssql/connect-overview)
- Drizzle 迁移基础 - [点此阅读](/docs/mssql/migrations)
</Prerequisites>


**Drizzle Kit** 是一个用于通过 Drizzle 管理 SQL 数据库迁移的 CLI 工具。
<Npm>
  -D drizzle-kit
</Npm>
<Callout type="warning">
请务必先阅读 Drizzle 的 [入门指南](/docs/mssql/get-started) 和 [迁移基础](/docs/mssql/migrations)，并选择最适合你业务需求的 SQL 迁移流程。 
</Callout>

基于你的 schema，Drizzle Kit 可以让你生成并运行 SQL 迁移文件， 
将 schema 直接推送到数据库，从数据库拉取 schema，启动 drizzle studio，并提供一些实用命令。
<Npx>
drizzle-kit generate
drizzle-kit migrate
drizzle-kit push
drizzle-kit pull
drizzle-kit check
drizzle-kit up
drizzle-kit studio
drizzle-kit export
</Npx>


|                                                      |                                                                                                                                                                    |
| :--------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`drizzle-kit generate`](/docs/mssql/drizzle-kit-generate) | 可根据你的 Drizzle schema 在声明时或后续变更时生成 SQL 迁移文件， [点此查看](/docs/mssql/drizzle-kit-generate)。       |
| [`drizzle-kit migrate`](/docs/mssql/drizzle-kit-migrate)   | 可将生成的 SQL 迁移文件应用到你的数据库， [点此查看](/docs/mssql/drizzle-kit-migrate)。                                                              |
| [`drizzle-kit pull`](/docs/mssql/drizzle-kit-pull)         | 可拉取（introspect）数据库 schema，将其转换为 Drizzle schema 并保存到你的代码库中， [点此查看](/docs/mssql/drizzle-kit-pull)                           |
| [`drizzle-kit push`](/docs/mssql/drizzle-kit-push)         | 可在声明时或后续 schema 变更时将你的 Drizzle schema 推送到数据库， [点此查看](/docs/mssql/drizzle-kit-push)                          |
| [`drizzle-kit studio`](/docs/mssql/drizzle-kit-studio)     | 将连接到你的数据库并启动 Drizzle Studio 的代理服务器，你可以用它方便地浏览数据库， [点此查看](/docs/mssql/drizzle-kit-studio) |
| [`drizzle-kit check`](/docs/mssql/drizzle-kit-check)       | 将遍历所有已生成的迁移，并检查生成的迁移是否存在任何竞态条件（冲突）， [点此查看](/docs/mssql/drizzle-kit-check)               |
| [`drizzle-kit up`](/docs/mssql/drizzle-kit-up)             | 用于升级之前生成的迁移快照， [点此查看](/docs/mssql/drizzle-kit-up)                                                                     |
| [`drizzle-kit export`](/docs/mssql/drizzle-kit-export)     | 用于将 TypeScript schema 转换为原始 SQL DDL 并输出出来 [点此查看](/docs/mssql/drizzle-kit-export)                                                                     |
<br/>

Drizzle Kit 通过 [drizzle.config.ts](/docs/mssql/drizzle-config-file) 配置文件或 CLI 参数进行配置。<br/>
至少需要提供 SQL `dialect` 和 `schema` 路径，以便 Drizzle Kit 知道如何生成迁移。
```
📦 <project root>
 ├ 📂 drizzle
 ├ 📂 src
 ├ 📜 .env
 ├ 📜 drizzle.config.ts  <--- Drizzle 配置文件
 ├ 📜 package.json
 └ 📜 tsconfig.json
```

<CodeTabs items={["simple config", "extended config"]}>
```ts
import { defineConfig } from "drizzle-kit";

export default defineConfig({
  dialect: "mssql",
  schema: "./src/schema.ts",
});
```
```ts
import { defineConfig } from "drizzle-kit";

export default defineConfig({
  out: "./drizzle",
  dialect: "mssql",
  schema: "./src/schema.ts",

  dbCredentials: {
    url: "./database/",
  },

  schemaFilter: "dbo",
  tablesFilter: "*",

  introspect: {
    casing: "camel",
  },

  migrations: {
    table: "__drizzle_migrations__",
    schema: "drizzle",
  },

  breakpoints: true,
  verbose: true,
});
```
</CodeTabs>

你可以通过 CLI 参数提供 Drizzle Kit 配置路径，这在你有多个数据库阶段、多个数据库，或同一个项目中存在不同数据库时非常有用：

<Npx>
  drizzle-kit push --config=drizzle-dev.drizzle.config
  drizzle-kit push --config=drizzle-prod.drizzle.config
</Npx>
```plaintext {5-6}
📦 <project root>
 ├ 📂 drizzle
 ├ 📂 src
 ├ 📜 .env
 ├ 📜 drizzle-dev.config.ts
 ├ 📜 drizzle-prod.config.ts
 ├ 📜 package.json
 └ 📜 tsconfig.json
```
