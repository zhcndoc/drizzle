import CodeTab from "@mdx/CodeTab.astro";
import CodeTabs from "@mdx/CodeTabs.astro";
import Section from "@mdx/Section.astro";
import Tab from "@mdx/Tab.astro";
import Tabs from "@mdx/Tabs.astro";
import Callout from "@mdx/Callout.astro";
import Prerequisites from "@mdx/Prerequisites.astro";
import Npx from "@mdx/Npx.astro";
import Dialects from "@mdx/Dialects.mdx"

# `drizzle-kit up`

<Prerequisites>
- 开始使用 Drizzle 和 `drizzle-kit` - [点此阅读](/docs/mssql/get-started)
- Drizzle schema 基础知识 - [点此阅读](/docs/mssql/sql-schema-declaration)
- 数据库连接基础知识 - [点此阅读](/docs/mssql/connect-overview)
- Drizzle migrations 基础知识 - [点此阅读](/docs/mssql/migrations)
- Drizzle Kit [概览](/docs/mssql/kit-overview) 和 [配置文件](/docs/mssql/drizzle-config-file)
- `drizzle-kit generate` 命令 - [点此阅读](/docs/mssql/drizzle-kit-generate)
</Prerequisites>

`drizzle-kit up` 命令允许你将 drizzle schema 快照升级到更新的版本。 
当我们引入会破坏兼容性的 schema JSON 快照变更并升级内部版本时，就需要使用它。

<br/>
<hr/>
<br/>

`drizzle-kit up` 命令要求你指定 `dialect` 参数，
你可以通过 [drizzle.config.ts](/docs/mssql/drizzle-config-file) 配置文件或通过 CLI 选项提供它

<CodeTabs items={["使用配置文件", "作为 CLI 选项"]}>
<Section>
```ts {5,8}
// drizzle.config.ts
import { defineConfig } from "drizzle-kit";

export default defineConfig({
  dialect: "mssql",
});
```
```shell
npx drizzle-kit up
```
</Section>
```shell
npx drizzle-kit up --dialect=mssql
```
</CodeTabs>

### 一个项目中的多个配置文件
你可以在项目中拥有多个配置文件，当你有多个数据库阶段或同一项目中有多个数据库时，这会非常有用：
<Npx>
  drizzle-kit migrate --config=drizzle-dev.config.ts
  drizzle-kit migrate --config=drizzle-prod.config.ts
</Npx>
```plaintext {5-6}
📦 <project root>
 ├ 📂 drizzle
 ├ 📂 src
 ├ 📜 .env
 ├ 📜 drizzle-dev.config.ts
 ├ 📜 drizzle-prod.config.ts
 ├ 📜 package.json
 └ 📜 tsconfig.json
```

### 扩展配置列表
我们建议通过 [drizzle.config.ts](/docs/mssql/drizzle-config-file) 文件来配置 `drizzle-kit`， 
但如有必要，你也可以通过 CLI 提供所有配置选项，例如在 CI/CD 流水线等场景中。
<rem025/>
|           |            |                                                                         |
| :-------- | :--------- | :---------------------------------------------------------------------- |
| `dialect` | `required` | 你正在使用的数据库方言。可以是 <Dialects/> 中的一种 |
| `out`     |            | migrations 文件夹，默认=`./drizzle`                                  |
| `config`  |            | 配置文件路径，默认=`drizzle.config.ts`                           |
<br/>
<Npx>
drizzle-kit up --dialect=mssql
drizzle-kit up --dialect=mssql --out=./migrations-folder
</Npx>

{/* TODO: update up gif */}
![](@/assets/gifs/up_mysql.gif)
