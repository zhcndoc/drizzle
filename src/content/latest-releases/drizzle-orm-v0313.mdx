---
title: DrizzleORM v0.31.3 发布
pubDate: 2024-07-08
description: Prisma-Drizzle 扩展
---

### Bug fixed

- 🛠️ 修复了在具有相同名称的不同架构中的表中的 RQB 行为
- 🛠️ 修复了[BUG]：在使用 RDS 数据 API 时类型提示不匹配 - #2097

### 新 Prisma-Drizzle 扩展  

```ts
import { PrismaClient } from '@prisma/client';
import { drizzle } from 'drizzle-orm/prisma/pg';
import { User } from './drizzle';

const prisma = new PrismaClient().$extends(drizzle());
const users = await prisma.$drizzle.select().from(User);
```

有关更多信息，请查看文档：https://orm.drizzle.team/docs/prisma
