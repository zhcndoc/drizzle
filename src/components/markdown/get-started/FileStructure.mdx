#### 基本文件结构

这是项目的基本文件结构。在 `src/db` 目录中，我们在 `schema.ts` 中有表定义。在 `drizzle` 文件夹中，有 SQL 迁移文件和快照。

```plaintext
📦 <project root>
 ├ 📂 drizzle
 ├ 📂 src
 │   ├ 📂 db
 │   │  └ 📜 schema.ts
 │   └ 📜 index.ts
 ├ 📜 .env
 ├ 📜 drizzle.config.ts
 ├ 📜 package.json
 └ 📜 tsconfig.json
```
